meta {
  name: Create Account
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/v1/auth/signup
  body: json
  auth: inherit
}

body:json {
  {
    "email": "user@example.com",
    "password": "string",
    "first_name": "string",
    "last_name": "string"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
