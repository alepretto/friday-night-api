meta {
  name: List Categories with Subcategories
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/finance/categories
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
