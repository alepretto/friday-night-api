meta {
  name: List Categories (1)
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/finance/categories
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
