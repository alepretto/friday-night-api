meta {
  name: List Tags
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/finance/tags?size=200
  body: none
  auth: bearer
}

params:query {
  size: 200
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
